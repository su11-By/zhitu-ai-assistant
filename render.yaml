services:
  - type: web
    name: zhitu-ai-server
    runtime: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node deploy.js
    envVars:
      - key: NODE_ENV
        value: production
